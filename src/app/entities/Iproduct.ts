export interface IProducts{
    id: number;
      name: string;
      price: number;
      img: string;
      quantity: number;
      cate_id: number;
      xem: number;
      description: string;
      ngay: string;
      an_hien: number;
}